[project]
name = "trustscore-mcp"
version = "0.1.0"
description = "Trust and reputation scores for AI agent service selection"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "aiohttp>=3.9.0",
    "aiosqlite>=0.19.0",
]

[project.scripts]
trustscore = "src.server:main"
